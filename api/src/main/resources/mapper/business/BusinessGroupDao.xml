<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.business.dao.BusinessGroupDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <sql id="Base_Column_List">
        group_id,
        mm_id,
        group_name,
        remarks,
        create_user_id,
        create_time,
        `status`
    </sql>
    <resultMap type="io.renren.modules.business.entity.BusinessGroupEntity" id="businessGroupMap">
        <result property="groupId" column="group_id"/>
        <result property="mmId" column="mm_id"/>
        <result property="groupName" column="group_name"/>
        <result property="remarks" column="remarks"/>
        <result property="createUserId" column="create_user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="status" column="status"/>
    </resultMap>

    <select id="queryNotButtonList" resultType="io.renren.modules.business.entity.BusinessGroupEntity">
        select * from business_group where status = 1
    </select>

<!--auto generated by MybatisCodeHelper on 2022-03-20-->


    <select id="findAllByCreateUserIdCount" resultType="java.lang.Integer">
        select
        count(*)
        FROM business_group bg
        LEFT JOIN sys_user_group ug ON ug.group_id = bg.group_id
        <where>
            <if test="createUserId != null">
                ug.user_id = #{createUserId} or bg.create_user_id = #{createUserId}
            </if>
        </where>
    </select>

<!--auto generated by MybatisCodeHelper on 2022-03-21-->
    <select id="findAllByCreateUserId" resultMap="businessGroupMap">
        select
        <include refid="Base_Column_List"/>
        from business_group
        <where>
            <if test="createUserId != null">
                and create_user_id=#{createUserId}
            </if>
        </where>
    </select>
</mapper>
